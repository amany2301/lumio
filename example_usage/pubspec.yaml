name: lumio_usage_example
description: Example project showing how to use Lumio in a Flutter app
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  lumio: ^0.0.1  # Add Lumio dependency
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
